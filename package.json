{
  "name": "bitrix24-vkvv4-bi-report",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "lint": "eslint .",
    "test": "node --test --experimental-test-coverage --test-coverage-include=lib/**/*.js --test-coverage-lines=70",
    "check": "npm run lint && npm test",
    "audit": "npm audit --audit-level=high"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "eslint": "^10.8.0"
  }
}
